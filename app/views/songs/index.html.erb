<h1>Songs</h1>
<ul style="list-style-type:none">
  <% @songs.each do |song| %>
    <li><a href="<%= song_path(song) %>"><%= song.artist.name %> -
        <%= song.title %></a>
    </li>
  <% end %>
</ul>
